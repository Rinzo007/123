"""Константы дедупликации, вынесенные из dedup_runtime.

Отдельный модуль, чтобы не создавать циклических импортов между
dedup_cache / dedup_analyze / dedup_network / dedup_matrix.
"""
import os

# Версия алгоритма анализа — меняется при изменении смысла полей кэша.
_DEDUP_ALGO_VERSION = "dedup-analyze-v2-stop-names"

# Порог числа кандидат-пар, после которого цикл покрытия распараллеливается
# по потокам (накладные расходы на малых сетях не окупаются).
_THREAD_PAIRS = 1_000
_THREAD_WORK = 200_000
"""Оценка работы (число линий × число партнёров) для входа в многопоточность."""

_CPU_COUNT = os.cpu_count() or 1

MAX_POINTS_PER_LINE = 2000
"""Максимум сэмплированных точек на линию в _route_cov_sampled (защита от OOM)."""

MAX_POINTS_PER_ROUTE = 50_000
"""Суммарный потолок сэмплированных точек маршрута в _route_cov_sampled.

Один маршрут может содержать много вариантов/линий, поэтому даже при
лимите на линию общее число точек способно взорваться (100 линий × 2000).
Превышение равномерно сжимает число точек на каждой линии (вариант A),
сохраняя плотность сэмплирования.
"""

_COV_MAX_CELLS = 1_000_000
"""Потолок ячеек bbox-матрицы (K × m) за один проход _route_cov (стабильность памяти)."""

_MAX_INTERSECTIONS = 200_000
"""Потолок GEOS-пересечений за один вызов intersection в _route_cov.

Даже при ограниченной bbox-матрице плотное совпадение может породить
миллионы попарных оверлеев; дробим их на куски, чтобы держать память
и время отклика стабильными.
"""

_QUERY_CHUNK = 20_000
"""Число точек за один запрос STRtree в _route_cov_sampled.

Плотная сеть даёт огромный результат пересечений; дробим запрос по точкам,
чтобы не держать в памяти сразу все (point_idx, buffer_idx) пары.
"""
